# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=todo_user
DB_PASSWORD=change-me-in-production
DB_NAME=todo_app

# Application Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
# IMPORTANTE: Genera una chiave sicura con: openssl rand -base64 32
# In produzione, usa un secret manager (AWS Secrets Manager, HashiCorp Vault, ecc.)
JWT_SECRET=generate-with-openssl-rand-base64-32-minimum-32-characters
